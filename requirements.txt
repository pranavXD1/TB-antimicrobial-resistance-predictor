fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
xgboost>=2.0
scikit-learn>=1.3
joblib>=1.3
pandas>=2.0
numpy>=1.24
# NOTE: src/serve/predict.py imports from src/data/vcf_fetch.py and
# src/data/download.py (annotate_token, parse_vcf). If those modules import
# anything beyond the above (e.g. pysam / cyvcf2 for VCF parsing), add it here —
# the VCF-upload path and even token annotation depend on it.
